<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.igonan.mindmapper.PetDogmapper"> <!-- 해당 mapper를 사용하려는 파일 경로-->


    <select id="mindAbanDogChangePetDogInsert" >
        insert into pet_dog(pet_num,pet_name,pet_age,pet_date,pet_sex,pet_size,pet_spec,pet_vac,pet_neut,pet_memo,pet_gallery)
        values (null,#{param1}, #{param2},#{param3}, #{param4},#{param5}, #{param6},#{param7},#{param8}, #{param9}, #{param10})

    </select>
    <select id="mindPetImageInsert">
        insert into pet_image(num,f_num,img_src)
        values (null,#{param1}, #{param2})
    </select>

</mapper>
